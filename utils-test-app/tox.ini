[flake8]
exclude = .git, *migrations*
max-line-length = 88
select = C,E,F,W,B,B950
ignore = E203, E231, E501, W503, W291, W293


[tox]
envlist = {py38, py39, py310}-django{40}, docs

[testenv]
setenv =
    DJANGO_SETTINGS_MODULE = testauth.settings_aa3

deps=
    requests-mock
    coverage
    factory_boy

commands=
    pip install -e ../../allianceauth-app-utils
    coverage run runtests.py -v 2
    coverage xml
    coverage report -m

[testenv:docs]
description = invoke sphinx-build to build the HTML docs
deps = -r{toxinidir}/../docs/requirements.txt
commands =
    pip install -e ../../allianceauth-app-utils --no-deps
    sphinx-build -d "{toxworkdir}/docs_doctree" ../docs "{toxworkdir}/docs_out" --color -W -bhtml {posargs}
